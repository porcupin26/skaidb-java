# JitPack builds the artifact from the tag on demand:
#   https://jitpack.io/#porcupin26/skaidb-java
jdk:
  - openjdk17
# ./mvnw pins Maven 3.9.9; JitPack's own Maven predates the plugins used here.
install:
  - ./mvnw -B -ntp install -DskipTests
